{
  "name": "issue",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "aspida && nuxt-ts",
    "build": "aspida && nuxt-ts build",
    "start": "nuxt-ts start",
    "generate": "aspida && nuxt-ts generate",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aspida/axios": "^0.10.2",
    "@nuxt/typescript-runtime": "^1.0.0",
    "@nuxtjs/axios": "^5.12.0",
    "nuxt": "^2.14.0"
  },
  "devDependencies": {
    "@nuxt/types": "^2.14.0",
    "@nuxt/typescript-build": "^2.0.2"
  }
}
